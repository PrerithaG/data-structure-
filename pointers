#include<stdio.h>
int main()
{
    int*p;
    int a=10;
    printf("value of a:%d\n",a);
    printf("address of a : %p\n",&a);
    printf("pointer(ptr)stores address : %p\n",ptr);
    printf("value pointed by ptr : %d\n",*ptr);
    return 0;
}
