#include<stdio.h>
int main()
{
    int a[] = {1,3,5,7,9,10};
    int n = sizeof a/sizeof (a[0]);
    int index = 6;
    int newval = 5;
    if(index>=0&&index<n)
    {
       a[index]=newval;
    for(int i=0;i<n;i++)
    printf("%d",a[i]);
    }
    else
    printf("worst");
}
